@model EvernestWeb.Models.ChangePwdModel

<h3>Change password</h3>

@using (Html.BeginForm("ChangePwd", "Account", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <fieldset>
        <div class="form-group">
            @Html.LabelFor(m => m.Password, "Current password")
            <p class="text-danger">
                @Html.ValidationMessageFor(m => m.Password)
            </p>
            @Html.PasswordFor(m => m.Password, new { @class = "form-control", @placeholder = "Current password" })
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.NewPassword, "New password")
            <p class="text-danger">
                @Html.ValidationMessageFor(m => m.NewPassword)
            </p>
            @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control", @placeholder = "New password" })
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.ConfirmPassword, "Confirm new password")
            <p class="text-danger">
                @Html.ValidationMessageFor(m => m.ConfirmPassword)
            </p>
            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", @placeholder = "Confirm new password" })
        </div>
        <input type="submit" class="btn btn-primary" value="Submit" />
    </fieldset>
}

@if (ViewBag.Status != null)
{
    <p>@ViewBag.Status</p>
}
